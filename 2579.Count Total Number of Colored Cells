class Solution:
    def coloredCells(self, n: int) -> int:        
        tot=0
        for i in range(n):
            if i!=n-1:
                tot+=2*(2*i+1)
            else:
                tot+=2*i+1
        return tot
           
